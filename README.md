# tinyraytracer in Typescript

Following https://github.com/ssloy/tinyraytracer but implementing a DOM Canvas.

## Running

Run `npm ci`, then `npm run compile` (or `npm run dev` to watch for changes) and open `index.html`.